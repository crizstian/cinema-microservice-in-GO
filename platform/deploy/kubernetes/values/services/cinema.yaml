# Cinema Service Values
# Static configuration - no Harness expressions except service.name and artifact.tag

serviceName: <+service.name>
version: <+artifact.tag>

port: 8003
dbName: cinema

dependencies: []

autoscaling:
  minReplicas: 1
  maxReplicas: 5

pdb:
  minAvailable: 1
