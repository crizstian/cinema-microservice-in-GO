# Booking Service Values
# Template with Harness expressions - values resolved at deploy time

serviceName: <+service.name>
version: <+artifact.tag>

port: <+serviceVariables.port>
dbName: <+serviceVariables.dbName>

dependencies:
  - seat-service
  - payment-service
  - showtime-service
  - notification-service

resources:
  requests:
    cpu: <+serviceVariables.cpu_request>
    memory: <+serviceVariables.mem_request>
  limits:
    cpu: <+serviceVariables.cpu_limit>
    memory: <+serviceVariables.mem_limit>

autoscaling:
  minReplicas: 2
  maxReplicas: 10

pdb:
  minAvailable: 1
